# Legacy code

This directory contains legacy reference code kept only to preserve prior useful logic and migration context.

Files in this directory:

- are not part of the active application
- must not be imported by production code
- may be used as reference for modular reimplementation inside `src/`

Quality tools may exclude this directory from strict checks while the migration is in progress.
